// meth-syntax probe: View > Problems (Ctrl+Shift+M), save to refresh compiler diagnostics.
//
// Regex layer: labeled `break` (unsupported in Methlang).
// Compiler layer: undefined identifier `ghost` (reaches this if `break outer` is diagnosed first).

function main() -> int32 {
  while (1) {
    break outer;
  }
  return ghost;
}
