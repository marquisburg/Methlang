struct S0 {
  a: int64;
  b: int64;
  c: int64;
}

struct S1 {
  a: int64;
  b: int64;
  c: int64;
}

struct S2 {
  a: int64;
  b: int64;
  c: int64;
}

struct S3 {
  a: int64;
  b: int64;
  c: int64;
}

struct S4 {
  a: int64;
  b: int64;
  c: int64;
}

struct S5 {
  a: int64;
  b: int64;
  c: int64;
}

struct S6 {
  a: int64;
  b: int64;
  c: int64;
}

struct S7 {
  a: int64;
  b: int64;
  c: int64;
}

struct S8 {
  a: int64;
  b: int64;
  c: int64;
}

struct S9 {
  a: int64;
  b: int64;
  c: int64;
}

struct S10 {
  a: int64;
  b: int64;
  c: int64;
}

struct S11 {
  a: int64;
  b: int64;
  c: int64;
}

struct S12 {
  a: int64;
  b: int64;
  c: int64;
}

struct S13 {
  a: int64;
  b: int64;
  c: int64;
}

struct S14 {
  a: int64;
  b: int64;
  c: int64;
}

struct S15 {
  a: int64;
  b: int64;
  c: int64;
}

function compute0(x: int64, y: int64) -> int64;
function compute1(x: int64, y: int64) -> int64;
function compute2(x: int64, y: int64) -> int64;
function compute3(x: int64, y: int64) -> int64;
function compute4(x: int64, y: int64) -> int64;
function compute5(x: int64, y: int64) -> int64;
function compute6(x: int64, y: int64) -> int64;
function compute7(x: int64, y: int64) -> int64;
function compute8(x: int64, y: int64) -> int64;
function compute9(x: int64, y: int64) -> int64;
function compute10(x: int64, y: int64) -> int64;
function compute11(x: int64, y: int64) -> int64;
function compute12(x: int64, y: int64) -> int64;
function compute13(x: int64, y: int64) -> int64;
function compute14(x: int64, y: int64) -> int64;
function compute15(x: int64, y: int64) -> int64;
function compute16(x: int64, y: int64) -> int64;
function compute17(x: int64, y: int64) -> int64;
function compute18(x: int64, y: int64) -> int64;
function compute19(x: int64, y: int64) -> int64;
function compute20(x: int64, y: int64) -> int64;
function compute21(x: int64, y: int64) -> int64;
function compute22(x: int64, y: int64) -> int64;
function compute23(x: int64, y: int64) -> int64;
function compute24(x: int64, y: int64) -> int64;
function compute25(x: int64, y: int64) -> int64;
function compute26(x: int64, y: int64) -> int64;
function compute27(x: int64, y: int64) -> int64;
function compute28(x: int64, y: int64) -> int64;
function compute29(x: int64, y: int64) -> int64;
function compute30(x: int64, y: int64) -> int64;
function compute31(x: int64, y: int64) -> int64;
function compute32(x: int64, y: int64) -> int64;
function compute33(x: int64, y: int64) -> int64;
function compute34(x: int64, y: int64) -> int64;
function compute35(x: int64, y: int64) -> int64;
function compute36(x: int64, y: int64) -> int64;
function compute37(x: int64, y: int64) -> int64;
function compute38(x: int64, y: int64) -> int64;
function compute39(x: int64, y: int64) -> int64;
function compute40(x: int64, y: int64) -> int64;
function compute41(x: int64, y: int64) -> int64;
function compute42(x: int64, y: int64) -> int64;
function compute43(x: int64, y: int64) -> int64;
function compute44(x: int64, y: int64) -> int64;
function compute45(x: int64, y: int64) -> int64;
function compute46(x: int64, y: int64) -> int64;
function compute47(x: int64, y: int64) -> int64;
function compute48(x: int64, y: int64) -> int64;
function compute49(x: int64, y: int64) -> int64;
function compute50(x: int64, y: int64) -> int64;
function compute51(x: int64, y: int64) -> int64;
function compute52(x: int64, y: int64) -> int64;
function compute53(x: int64, y: int64) -> int64;
function compute54(x: int64, y: int64) -> int64;
function compute55(x: int64, y: int64) -> int64;
function compute56(x: int64, y: int64) -> int64;
function compute57(x: int64, y: int64) -> int64;
function compute58(x: int64, y: int64) -> int64;
function compute59(x: int64, y: int64) -> int64;
function compute60(x: int64, y: int64) -> int64;
function compute61(x: int64, y: int64) -> int64;
function compute62(x: int64, y: int64) -> int64;
function compute63(x: int64, y: int64) -> int64;
function compute64(x: int64, y: int64) -> int64;
function compute65(x: int64, y: int64) -> int64;
function compute66(x: int64, y: int64) -> int64;
function compute67(x: int64, y: int64) -> int64;
function compute68(x: int64, y: int64) -> int64;
function compute69(x: int64, y: int64) -> int64;
function compute70(x: int64, y: int64) -> int64;
function compute71(x: int64, y: int64) -> int64;
function compute72(x: int64, y: int64) -> int64;
function compute73(x: int64, y: int64) -> int64;
function compute74(x: int64, y: int64) -> int64;
function compute75(x: int64, y: int64) -> int64;
function compute76(x: int64, y: int64) -> int64;
function compute77(x: int64, y: int64) -> int64;
function compute78(x: int64, y: int64) -> int64;
function compute79(x: int64, y: int64) -> int64;
function compute80(x: int64, y: int64) -> int64;
function compute81(x: int64, y: int64) -> int64;
function compute82(x: int64, y: int64) -> int64;
function compute83(x: int64, y: int64) -> int64;
function compute84(x: int64, y: int64) -> int64;
function compute85(x: int64, y: int64) -> int64;
function compute86(x: int64, y: int64) -> int64;
function compute87(x: int64, y: int64) -> int64;
function compute88(x: int64, y: int64) -> int64;
function compute89(x: int64, y: int64) -> int64;
function compute90(x: int64, y: int64) -> int64;
function compute91(x: int64, y: int64) -> int64;
function compute92(x: int64, y: int64) -> int64;
function compute93(x: int64, y: int64) -> int64;
function compute94(x: int64, y: int64) -> int64;
function compute95(x: int64, y: int64) -> int64;
function compute96(x: int64, y: int64) -> int64;
function compute97(x: int64, y: int64) -> int64;
function compute98(x: int64, y: int64) -> int64;
function compute99(x: int64, y: int64) -> int64;
function compute100(x: int64, y: int64) -> int64;
function compute101(x: int64, y: int64) -> int64;
function compute102(x: int64, y: int64) -> int64;
function compute103(x: int64, y: int64) -> int64;
function compute104(x: int64, y: int64) -> int64;
function compute105(x: int64, y: int64) -> int64;
function compute106(x: int64, y: int64) -> int64;
function compute107(x: int64, y: int64) -> int64;
function compute108(x: int64, y: int64) -> int64;
function compute109(x: int64, y: int64) -> int64;
function compute110(x: int64, y: int64) -> int64;
function compute111(x: int64, y: int64) -> int64;
function compute112(x: int64, y: int64) -> int64;
function compute113(x: int64, y: int64) -> int64;
function compute114(x: int64, y: int64) -> int64;
function compute115(x: int64, y: int64) -> int64;
function compute116(x: int64, y: int64) -> int64;
function compute117(x: int64, y: int64) -> int64;
function compute118(x: int64, y: int64) -> int64;
function compute119(x: int64, y: int64) -> int64;
function compute120(x: int64, y: int64) -> int64;
function compute121(x: int64, y: int64) -> int64;
function compute122(x: int64, y: int64) -> int64;
function compute123(x: int64, y: int64) -> int64;
function compute124(x: int64, y: int64) -> int64;
function compute125(x: int64, y: int64) -> int64;
function compute126(x: int64, y: int64) -> int64;
function compute127(x: int64, y: int64) -> int64;
function compute128(x: int64, y: int64) -> int64;
function compute129(x: int64, y: int64) -> int64;
function compute130(x: int64, y: int64) -> int64;
function compute131(x: int64, y: int64) -> int64;
function compute132(x: int64, y: int64) -> int64;
function compute133(x: int64, y: int64) -> int64;
function compute134(x: int64, y: int64) -> int64;
function compute135(x: int64, y: int64) -> int64;
function compute136(x: int64, y: int64) -> int64;
function compute137(x: int64, y: int64) -> int64;
function compute138(x: int64, y: int64) -> int64;
function compute139(x: int64, y: int64) -> int64;
function compute140(x: int64, y: int64) -> int64;
function compute141(x: int64, y: int64) -> int64;
function compute142(x: int64, y: int64) -> int64;
function compute143(x: int64, y: int64) -> int64;
function compute144(x: int64, y: int64) -> int64;
function compute145(x: int64, y: int64) -> int64;
function compute146(x: int64, y: int64) -> int64;
function compute147(x: int64, y: int64) -> int64;
function compute148(x: int64, y: int64) -> int64;
function compute149(x: int64, y: int64) -> int64;
function compute150(x: int64, y: int64) -> int64;
function compute151(x: int64, y: int64) -> int64;
function compute152(x: int64, y: int64) -> int64;
function compute153(x: int64, y: int64) -> int64;
function compute154(x: int64, y: int64) -> int64;
function compute155(x: int64, y: int64) -> int64;
function compute156(x: int64, y: int64) -> int64;
function compute157(x: int64, y: int64) -> int64;
function compute158(x: int64, y: int64) -> int64;
function compute159(x: int64, y: int64) -> int64;
function compute160(x: int64, y: int64) -> int64;
function compute161(x: int64, y: int64) -> int64;
function compute162(x: int64, y: int64) -> int64;
function compute163(x: int64, y: int64) -> int64;
function compute164(x: int64, y: int64) -> int64;
function compute165(x: int64, y: int64) -> int64;
function compute166(x: int64, y: int64) -> int64;
function compute167(x: int64, y: int64) -> int64;
function compute168(x: int64, y: int64) -> int64;
function compute169(x: int64, y: int64) -> int64;
function compute170(x: int64, y: int64) -> int64;
function compute171(x: int64, y: int64) -> int64;
function compute172(x: int64, y: int64) -> int64;
function compute173(x: int64, y: int64) -> int64;
function compute174(x: int64, y: int64) -> int64;
function compute175(x: int64, y: int64) -> int64;
function compute176(x: int64, y: int64) -> int64;
function compute177(x: int64, y: int64) -> int64;
function compute178(x: int64, y: int64) -> int64;
function compute179(x: int64, y: int64) -> int64;
function compute180(x: int64, y: int64) -> int64;
function compute181(x: int64, y: int64) -> int64;
function compute182(x: int64, y: int64) -> int64;
function compute183(x: int64, y: int64) -> int64;
function compute184(x: int64, y: int64) -> int64;
function compute185(x: int64, y: int64) -> int64;
function compute186(x: int64, y: int64) -> int64;
function compute187(x: int64, y: int64) -> int64;
function compute188(x: int64, y: int64) -> int64;
function compute189(x: int64, y: int64) -> int64;
function compute190(x: int64, y: int64) -> int64;
function compute191(x: int64, y: int64) -> int64;
function compute192(x: int64, y: int64) -> int64;
function compute193(x: int64, y: int64) -> int64;
function compute194(x: int64, y: int64) -> int64;
function compute195(x: int64, y: int64) -> int64;
function compute196(x: int64, y: int64) -> int64;
function compute197(x: int64, y: int64) -> int64;
function compute198(x: int64, y: int64) -> int64;
function compute199(x: int64, y: int64) -> int64;
function compute200(x: int64, y: int64) -> int64;
function compute201(x: int64, y: int64) -> int64;
function compute202(x: int64, y: int64) -> int64;
function compute203(x: int64, y: int64) -> int64;
function compute204(x: int64, y: int64) -> int64;
function compute205(x: int64, y: int64) -> int64;
function compute206(x: int64, y: int64) -> int64;
function compute207(x: int64, y: int64) -> int64;
function compute208(x: int64, y: int64) -> int64;
function compute209(x: int64, y: int64) -> int64;
function compute210(x: int64, y: int64) -> int64;
function compute211(x: int64, y: int64) -> int64;
function compute212(x: int64, y: int64) -> int64;
function compute213(x: int64, y: int64) -> int64;
function compute214(x: int64, y: int64) -> int64;
function compute215(x: int64, y: int64) -> int64;
function compute216(x: int64, y: int64) -> int64;
function compute217(x: int64, y: int64) -> int64;
function compute218(x: int64, y: int64) -> int64;
function compute219(x: int64, y: int64) -> int64;
function compute220(x: int64, y: int64) -> int64;
function compute221(x: int64, y: int64) -> int64;
function compute222(x: int64, y: int64) -> int64;
function compute223(x: int64, y: int64) -> int64;
function compute224(x: int64, y: int64) -> int64;
function compute225(x: int64, y: int64) -> int64;
function compute226(x: int64, y: int64) -> int64;
function compute227(x: int64, y: int64) -> int64;
function compute228(x: int64, y: int64) -> int64;
function compute229(x: int64, y: int64) -> int64;
function compute230(x: int64, y: int64) -> int64;
function compute231(x: int64, y: int64) -> int64;
function compute232(x: int64, y: int64) -> int64;
function compute233(x: int64, y: int64) -> int64;
function compute234(x: int64, y: int64) -> int64;
function compute235(x: int64, y: int64) -> int64;
function compute236(x: int64, y: int64) -> int64;
function compute237(x: int64, y: int64) -> int64;
function compute238(x: int64, y: int64) -> int64;
function compute239(x: int64, y: int64) -> int64;
function compute240(x: int64, y: int64) -> int64;
function compute241(x: int64, y: int64) -> int64;
function compute242(x: int64, y: int64) -> int64;
function compute243(x: int64, y: int64) -> int64;
function compute244(x: int64, y: int64) -> int64;
function compute245(x: int64, y: int64) -> int64;
function compute246(x: int64, y: int64) -> int64;
function compute247(x: int64, y: int64) -> int64;
function compute248(x: int64, y: int64) -> int64;
function compute249(x: int64, y: int64) -> int64;
function compute250(x: int64, y: int64) -> int64;
function compute251(x: int64, y: int64) -> int64;
function compute252(x: int64, y: int64) -> int64;
function compute253(x: int64, y: int64) -> int64;
function compute254(x: int64, y: int64) -> int64;
function compute255(x: int64, y: int64) -> int64;
function compute256(x: int64, y: int64) -> int64;
function compute257(x: int64, y: int64) -> int64;
function compute258(x: int64, y: int64) -> int64;
function compute259(x: int64, y: int64) -> int64;
function compute260(x: int64, y: int64) -> int64;
function compute261(x: int64, y: int64) -> int64;
function compute262(x: int64, y: int64) -> int64;
function compute263(x: int64, y: int64) -> int64;
function compute264(x: int64, y: int64) -> int64;
function compute265(x: int64, y: int64) -> int64;
function compute266(x: int64, y: int64) -> int64;
function compute267(x: int64, y: int64) -> int64;
function compute268(x: int64, y: int64) -> int64;
function compute269(x: int64, y: int64) -> int64;
function compute270(x: int64, y: int64) -> int64;
function compute271(x: int64, y: int64) -> int64;
function compute272(x: int64, y: int64) -> int64;
function compute273(x: int64, y: int64) -> int64;
function compute274(x: int64, y: int64) -> int64;
function compute275(x: int64, y: int64) -> int64;
function compute276(x: int64, y: int64) -> int64;
function compute277(x: int64, y: int64) -> int64;
function compute278(x: int64, y: int64) -> int64;
function compute279(x: int64, y: int64) -> int64;
function compute280(x: int64, y: int64) -> int64;
function compute281(x: int64, y: int64) -> int64;
function compute282(x: int64, y: int64) -> int64;
function compute283(x: int64, y: int64) -> int64;
function compute284(x: int64, y: int64) -> int64;
function compute285(x: int64, y: int64) -> int64;
function compute286(x: int64, y: int64) -> int64;
function compute287(x: int64, y: int64) -> int64;
function compute288(x: int64, y: int64) -> int64;
function compute289(x: int64, y: int64) -> int64;
function compute290(x: int64, y: int64) -> int64;
function compute291(x: int64, y: int64) -> int64;
function compute292(x: int64, y: int64) -> int64;
function compute293(x: int64, y: int64) -> int64;
function compute294(x: int64, y: int64) -> int64;
function compute295(x: int64, y: int64) -> int64;
function compute296(x: int64, y: int64) -> int64;
function compute297(x: int64, y: int64) -> int64;
function compute298(x: int64, y: int64) -> int64;
function compute299(x: int64, y: int64) -> int64;
function compute300(x: int64, y: int64) -> int64;
function compute301(x: int64, y: int64) -> int64;
function compute302(x: int64, y: int64) -> int64;
function compute303(x: int64, y: int64) -> int64;
function compute304(x: int64, y: int64) -> int64;
function compute305(x: int64, y: int64) -> int64;
function compute306(x: int64, y: int64) -> int64;
function compute307(x: int64, y: int64) -> int64;
function compute308(x: int64, y: int64) -> int64;
function compute309(x: int64, y: int64) -> int64;
function compute310(x: int64, y: int64) -> int64;
function compute311(x: int64, y: int64) -> int64;
function compute312(x: int64, y: int64) -> int64;
function compute313(x: int64, y: int64) -> int64;
function compute314(x: int64, y: int64) -> int64;
function compute315(x: int64, y: int64) -> int64;
function compute316(x: int64, y: int64) -> int64;
function compute317(x: int64, y: int64) -> int64;
function compute318(x: int64, y: int64) -> int64;
function compute319(x: int64, y: int64) -> int64;
function compute320(x: int64, y: int64) -> int64;
function compute321(x: int64, y: int64) -> int64;
function compute322(x: int64, y: int64) -> int64;
function compute323(x: int64, y: int64) -> int64;
function compute324(x: int64, y: int64) -> int64;
function compute325(x: int64, y: int64) -> int64;
function compute326(x: int64, y: int64) -> int64;
function compute327(x: int64, y: int64) -> int64;
function compute328(x: int64, y: int64) -> int64;
function compute329(x: int64, y: int64) -> int64;
function compute330(x: int64, y: int64) -> int64;
function compute331(x: int64, y: int64) -> int64;
function compute332(x: int64, y: int64) -> int64;
function compute333(x: int64, y: int64) -> int64;
function compute334(x: int64, y: int64) -> int64;
function compute335(x: int64, y: int64) -> int64;
function compute336(x: int64, y: int64) -> int64;
function compute337(x: int64, y: int64) -> int64;
function compute338(x: int64, y: int64) -> int64;
function compute339(x: int64, y: int64) -> int64;
function compute340(x: int64, y: int64) -> int64;
function compute341(x: int64, y: int64) -> int64;
function compute342(x: int64, y: int64) -> int64;
function compute343(x: int64, y: int64) -> int64;
function compute344(x: int64, y: int64) -> int64;
function compute345(x: int64, y: int64) -> int64;
function compute346(x: int64, y: int64) -> int64;
function compute347(x: int64, y: int64) -> int64;
function compute348(x: int64, y: int64) -> int64;
function compute349(x: int64, y: int64) -> int64;
function compute350(x: int64, y: int64) -> int64;
function compute351(x: int64, y: int64) -> int64;
function compute352(x: int64, y: int64) -> int64;
function compute353(x: int64, y: int64) -> int64;
function compute354(x: int64, y: int64) -> int64;
function compute355(x: int64, y: int64) -> int64;
function compute356(x: int64, y: int64) -> int64;
function compute357(x: int64, y: int64) -> int64;
function compute358(x: int64, y: int64) -> int64;
function compute359(x: int64, y: int64) -> int64;
function compute360(x: int64, y: int64) -> int64;
function compute361(x: int64, y: int64) -> int64;
function compute362(x: int64, y: int64) -> int64;
function compute363(x: int64, y: int64) -> int64;
function compute364(x: int64, y: int64) -> int64;
function compute365(x: int64, y: int64) -> int64;
function compute366(x: int64, y: int64) -> int64;
function compute367(x: int64, y: int64) -> int64;
function compute368(x: int64, y: int64) -> int64;
function compute369(x: int64, y: int64) -> int64;
function compute370(x: int64, y: int64) -> int64;
function compute371(x: int64, y: int64) -> int64;
function compute372(x: int64, y: int64) -> int64;
function compute373(x: int64, y: int64) -> int64;
function compute374(x: int64, y: int64) -> int64;
function compute375(x: int64, y: int64) -> int64;
function compute376(x: int64, y: int64) -> int64;
function compute377(x: int64, y: int64) -> int64;
function compute378(x: int64, y: int64) -> int64;
function compute379(x: int64, y: int64) -> int64;
function compute380(x: int64, y: int64) -> int64;
function compute381(x: int64, y: int64) -> int64;
function compute382(x: int64, y: int64) -> int64;
function compute383(x: int64, y: int64) -> int64;
function compute384(x: int64, y: int64) -> int64;
function compute385(x: int64, y: int64) -> int64;
function compute386(x: int64, y: int64) -> int64;
function compute387(x: int64, y: int64) -> int64;
function compute388(x: int64, y: int64) -> int64;
function compute389(x: int64, y: int64) -> int64;
function compute390(x: int64, y: int64) -> int64;
function compute391(x: int64, y: int64) -> int64;
function compute392(x: int64, y: int64) -> int64;
function compute393(x: int64, y: int64) -> int64;
function compute394(x: int64, y: int64) -> int64;
function compute395(x: int64, y: int64) -> int64;
function compute396(x: int64, y: int64) -> int64;
function compute397(x: int64, y: int64) -> int64;
function compute398(x: int64, y: int64) -> int64;
function compute399(x: int64, y: int64) -> int64;
function compute400(x: int64, y: int64) -> int64;
function compute401(x: int64, y: int64) -> int64;
function compute402(x: int64, y: int64) -> int64;
function compute403(x: int64, y: int64) -> int64;
function compute404(x: int64, y: int64) -> int64;
function compute405(x: int64, y: int64) -> int64;
function compute406(x: int64, y: int64) -> int64;
function compute407(x: int64, y: int64) -> int64;
function compute408(x: int64, y: int64) -> int64;
function compute409(x: int64, y: int64) -> int64;
function compute410(x: int64, y: int64) -> int64;
function compute411(x: int64, y: int64) -> int64;
function compute412(x: int64, y: int64) -> int64;
function compute413(x: int64, y: int64) -> int64;
function compute414(x: int64, y: int64) -> int64;
function compute415(x: int64, y: int64) -> int64;
function compute416(x: int64, y: int64) -> int64;
function compute417(x: int64, y: int64) -> int64;
function compute418(x: int64, y: int64) -> int64;
function compute419(x: int64, y: int64) -> int64;
function compute420(x: int64, y: int64) -> int64;
function compute421(x: int64, y: int64) -> int64;
function compute422(x: int64, y: int64) -> int64;
function compute423(x: int64, y: int64) -> int64;
function compute424(x: int64, y: int64) -> int64;
function compute425(x: int64, y: int64) -> int64;
function compute426(x: int64, y: int64) -> int64;
function compute427(x: int64, y: int64) -> int64;
function compute428(x: int64, y: int64) -> int64;
function compute429(x: int64, y: int64) -> int64;
function compute430(x: int64, y: int64) -> int64;
function compute431(x: int64, y: int64) -> int64;
function compute432(x: int64, y: int64) -> int64;
function compute433(x: int64, y: int64) -> int64;
function compute434(x: int64, y: int64) -> int64;
function compute435(x: int64, y: int64) -> int64;
function compute436(x: int64, y: int64) -> int64;
function compute437(x: int64, y: int64) -> int64;
function compute438(x: int64, y: int64) -> int64;
function compute439(x: int64, y: int64) -> int64;
function compute440(x: int64, y: int64) -> int64;
function compute441(x: int64, y: int64) -> int64;
function compute442(x: int64, y: int64) -> int64;
function compute443(x: int64, y: int64) -> int64;
function compute444(x: int64, y: int64) -> int64;
function compute445(x: int64, y: int64) -> int64;
function compute446(x: int64, y: int64) -> int64;
function compute447(x: int64, y: int64) -> int64;
function compute448(x: int64, y: int64) -> int64;
function compute449(x: int64, y: int64) -> int64;
function compute450(x: int64, y: int64) -> int64;
function compute451(x: int64, y: int64) -> int64;
function compute452(x: int64, y: int64) -> int64;
function compute453(x: int64, y: int64) -> int64;
function compute454(x: int64, y: int64) -> int64;
function compute455(x: int64, y: int64) -> int64;
function compute456(x: int64, y: int64) -> int64;
function compute457(x: int64, y: int64) -> int64;
function compute458(x: int64, y: int64) -> int64;
function compute459(x: int64, y: int64) -> int64;
function compute460(x: int64, y: int64) -> int64;
function compute461(x: int64, y: int64) -> int64;
function compute462(x: int64, y: int64) -> int64;
function compute463(x: int64, y: int64) -> int64;
function compute464(x: int64, y: int64) -> int64;
function compute465(x: int64, y: int64) -> int64;
function compute466(x: int64, y: int64) -> int64;
function compute467(x: int64, y: int64) -> int64;
function compute468(x: int64, y: int64) -> int64;
function compute469(x: int64, y: int64) -> int64;
function compute470(x: int64, y: int64) -> int64;
function compute471(x: int64, y: int64) -> int64;
function compute472(x: int64, y: int64) -> int64;
function compute473(x: int64, y: int64) -> int64;
function compute474(x: int64, y: int64) -> int64;
function compute475(x: int64, y: int64) -> int64;
function compute476(x: int64, y: int64) -> int64;
function compute477(x: int64, y: int64) -> int64;
function compute478(x: int64, y: int64) -> int64;
function compute479(x: int64, y: int64) -> int64;
function compute480(x: int64, y: int64) -> int64;
function compute481(x: int64, y: int64) -> int64;
function compute482(x: int64, y: int64) -> int64;
function compute483(x: int64, y: int64) -> int64;
function compute484(x: int64, y: int64) -> int64;
function compute485(x: int64, y: int64) -> int64;
function compute486(x: int64, y: int64) -> int64;
function compute487(x: int64, y: int64) -> int64;
function compute488(x: int64, y: int64) -> int64;
function compute489(x: int64, y: int64) -> int64;
function compute490(x: int64, y: int64) -> int64;
function compute491(x: int64, y: int64) -> int64;
function compute492(x: int64, y: int64) -> int64;
function compute493(x: int64, y: int64) -> int64;
function compute494(x: int64, y: int64) -> int64;
function compute495(x: int64, y: int64) -> int64;
function compute496(x: int64, y: int64) -> int64;
function compute497(x: int64, y: int64) -> int64;
function compute498(x: int64, y: int64) -> int64;
function compute499(x: int64, y: int64) -> int64;
function compute500(x: int64, y: int64) -> int64;
function compute501(x: int64, y: int64) -> int64;
function compute502(x: int64, y: int64) -> int64;
function compute503(x: int64, y: int64) -> int64;
function compute504(x: int64, y: int64) -> int64;
function compute505(x: int64, y: int64) -> int64;
function compute506(x: int64, y: int64) -> int64;
function compute507(x: int64, y: int64) -> int64;
function compute508(x: int64, y: int64) -> int64;
function compute509(x: int64, y: int64) -> int64;
function compute510(x: int64, y: int64) -> int64;
function compute511(x: int64, y: int64) -> int64;
function compute512(x: int64, y: int64) -> int64;
function compute513(x: int64, y: int64) -> int64;
function compute514(x: int64, y: int64) -> int64;
function compute515(x: int64, y: int64) -> int64;
function compute516(x: int64, y: int64) -> int64;
function compute517(x: int64, y: int64) -> int64;
function compute518(x: int64, y: int64) -> int64;
function compute519(x: int64, y: int64) -> int64;

function compute0(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + (k * v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute1(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + (k * v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute2(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute1(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute3(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute1(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute4(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute2(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute5(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute2(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute6(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute3(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute7(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute3(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute8(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute4(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute9(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute4(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute10(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute5(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute11(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute5(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute12(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute6(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute13(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute6(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute14(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute7(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute15(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute7(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute16(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute8(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute17(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute8(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute18(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute9(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute19(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute9(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute20(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute10(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute21(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute10(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute22(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute11(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute23(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute11(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute24(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute12(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute25(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute12(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute26(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute13(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute27(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute13(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute28(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute14(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute29(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute14(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute30(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute15(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute31(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute15(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute32(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute16(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute33(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute16(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute34(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute17(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute35(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute17(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute36(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute18(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute37(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute18(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute38(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute19(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute39(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute19(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute40(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute20(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute41(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute20(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute42(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute21(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute43(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute21(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute44(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute22(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute45(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute22(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute46(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute23(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute47(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute23(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute48(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute24(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute49(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute24(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute50(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute25(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute51(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute25(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute52(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute26(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute53(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute26(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute54(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute27(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute55(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute27(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute56(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute28(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute57(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute28(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute58(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute29(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute59(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute29(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute60(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute30(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute61(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute30(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute62(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute31(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute63(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute31(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute64(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute32(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute65(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute32(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute66(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute33(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute67(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute33(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute68(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute34(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute69(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute34(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute70(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute35(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute71(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute35(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute72(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute36(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute73(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute36(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute74(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute37(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute75(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute37(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute76(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute38(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute77(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute38(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute78(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute39(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute79(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute39(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute80(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute40(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute81(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute40(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute82(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute41(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute83(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute41(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute84(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute42(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute85(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute42(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute86(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute43(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute87(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute43(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute88(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute44(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute89(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute44(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute90(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute45(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute91(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute45(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute92(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute46(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute93(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute46(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute94(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute47(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute95(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute47(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute96(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute48(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute97(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute48(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute98(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute49(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute99(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute49(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute100(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute50(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute101(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute50(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute102(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute51(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute103(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute51(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute104(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute52(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute105(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute52(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute106(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute53(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute107(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute53(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute108(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute54(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute109(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute54(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute110(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute55(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute111(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute55(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute112(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute56(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute113(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute56(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute114(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute57(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute115(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute57(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute116(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute58(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute117(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute58(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute118(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute59(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute119(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute59(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute120(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute60(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute121(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute60(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute122(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute61(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute123(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute61(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute124(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute62(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute125(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute62(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute126(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute63(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute127(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute63(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute128(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute64(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute129(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute64(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute130(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute65(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute131(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute65(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute132(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute66(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute133(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute66(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute134(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute67(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute135(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute67(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute136(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute68(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute137(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute68(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute138(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute69(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute139(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute69(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute140(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute70(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute141(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute70(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute142(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute71(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute143(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute71(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute144(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute72(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute145(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute72(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute146(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute73(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute147(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute73(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute148(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute74(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute149(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute74(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute150(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute75(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute151(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute75(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute152(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute76(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute153(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute76(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute154(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute77(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute155(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute77(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute156(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute78(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute157(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute78(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute158(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute79(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute159(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute79(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute160(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute80(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute161(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute80(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute162(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute81(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute163(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute81(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute164(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute82(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute165(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute82(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute166(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute83(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute167(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute83(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute168(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute84(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute169(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute84(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute170(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute85(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute171(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute85(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute172(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute86(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute173(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute86(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute174(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute87(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute175(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute87(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute176(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute88(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute177(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute88(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute178(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute89(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute179(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute89(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute180(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute90(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute181(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute90(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute182(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute91(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute183(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute91(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute184(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute92(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute185(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute92(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute186(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute93(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute187(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute93(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute188(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute94(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute189(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute94(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute190(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute95(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute191(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute95(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute192(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute96(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute193(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute96(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute194(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute97(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute195(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute97(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute196(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute98(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute197(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute98(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute198(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute99(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute199(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute99(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute200(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute100(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute201(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute100(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute202(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute101(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute203(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute101(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute204(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute102(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute205(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute102(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute206(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute103(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute207(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute103(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute208(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute104(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute209(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute104(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute210(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute105(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute211(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute105(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute212(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute106(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute213(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute106(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute214(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute107(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute215(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute107(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute216(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute108(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute217(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute108(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute218(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute109(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute219(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute109(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute220(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute110(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute221(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute110(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute222(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute111(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute223(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute111(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute224(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute112(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute225(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute112(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute226(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute113(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute227(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute113(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute228(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute114(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute229(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute114(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute230(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute115(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute231(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute115(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute232(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute116(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute233(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute116(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute234(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute117(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute235(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute117(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute236(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute118(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute237(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute118(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute238(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute119(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute239(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute119(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute240(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute120(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute241(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute120(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute242(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute121(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute243(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute121(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute244(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute122(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute245(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute122(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute246(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute123(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute247(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute123(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute248(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute124(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute249(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute124(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute250(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute125(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute251(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute125(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute252(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute126(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute253(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute126(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute254(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute127(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute255(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute127(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute256(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute128(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute257(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute128(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute258(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute129(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute259(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute129(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute260(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute130(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute261(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute130(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute262(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute131(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute263(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute131(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute264(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute132(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute265(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute132(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute266(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute133(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute267(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute133(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute268(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute134(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute269(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute134(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute270(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute135(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute271(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute135(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute272(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute136(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute273(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute136(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute274(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute137(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute275(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute137(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute276(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute138(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute277(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute138(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute278(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute139(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute279(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute139(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute280(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute140(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute281(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute140(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute282(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute141(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute283(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute141(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute284(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute142(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute285(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute142(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute286(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute143(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute287(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute143(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute288(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute144(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute289(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute144(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute290(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute145(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute291(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute145(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute292(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute146(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute293(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute146(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute294(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute147(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute295(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute147(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute296(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute148(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute297(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute148(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute298(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute149(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute299(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute149(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute300(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute150(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute301(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute150(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute302(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute151(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute303(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute151(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute304(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute152(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute305(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute152(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute306(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute153(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute307(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute153(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute308(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute154(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute309(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute154(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute310(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute155(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute311(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute155(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute312(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute156(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute313(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute156(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute314(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute157(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute315(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute157(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute316(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute158(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute317(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute158(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute318(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute159(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute319(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute159(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute320(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute160(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute321(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute160(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute322(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute161(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute323(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute161(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute324(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute162(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute325(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute162(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute326(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute163(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute327(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute163(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute328(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute164(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute329(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute164(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute330(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute165(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute331(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute165(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute332(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute166(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute333(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute166(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute334(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute167(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute335(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute167(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute336(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute168(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute337(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute168(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute338(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute169(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute339(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute169(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute340(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute170(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute341(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute170(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute342(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute171(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute343(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute171(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute344(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute172(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute345(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute172(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute346(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute173(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute347(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute173(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute348(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute174(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute349(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute174(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute350(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute175(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute351(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute175(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute352(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute176(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute353(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute176(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute354(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute177(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute355(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute177(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute356(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute178(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute357(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute178(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute358(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute179(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute359(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute179(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute360(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute180(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute361(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute180(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute362(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute181(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute363(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute181(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute364(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute182(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute365(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute182(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute366(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute183(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute367(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute183(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute368(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute184(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute369(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute184(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute370(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute185(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute371(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute185(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute372(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute186(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute373(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute186(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute374(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute187(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute375(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute187(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute376(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute188(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute377(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute188(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute378(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute189(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute379(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute189(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute380(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute190(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute381(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute190(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute382(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute191(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute383(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute191(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute384(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute192(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute385(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute192(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute386(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute193(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute387(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute193(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute388(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute194(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute389(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute194(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute390(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute195(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute391(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute195(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute392(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute196(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute393(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute196(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute394(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute197(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute395(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute197(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute396(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute198(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute397(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute198(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute398(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute199(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute399(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute199(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute400(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute200(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute401(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute200(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute402(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute201(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute403(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute201(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute404(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute202(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute405(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute202(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute406(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute203(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute407(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute203(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute408(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute204(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute409(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute204(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute410(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute205(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute411(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute205(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute412(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute206(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute413(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute206(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute414(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute207(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute415(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute207(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute416(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute208(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute417(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute208(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute418(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute209(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute419(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute209(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute420(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute210(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute421(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute210(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute422(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute211(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute423(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute211(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute424(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute212(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute425(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute212(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute426(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute213(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute427(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute213(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute428(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute214(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute429(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute214(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute430(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute215(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute431(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute215(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute432(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute216(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute433(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute216(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute434(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute217(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute435(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute217(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute436(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute218(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute437(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute218(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute438(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute219(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute439(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute219(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute440(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute220(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute441(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute220(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute442(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute221(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute443(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute221(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute444(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute222(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute445(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute222(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute446(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute223(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute447(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute223(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute448(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute224(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute449(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute224(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute450(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute225(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute451(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute225(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute452(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute226(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute453(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute226(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute454(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute227(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute455(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute227(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute456(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute228(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute457(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute228(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute458(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute229(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute459(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute229(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute460(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute230(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute461(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute230(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute462(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute231(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute463(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute231(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute464(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute232(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute465(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute232(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute466(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute233(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute467(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute233(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute468(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute234(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute469(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute234(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute470(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute235(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute471(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute235(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute472(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute236(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute473(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute236(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute474(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute237(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute475(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute237(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute476(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute238(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute477(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute238(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute478(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute239(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute479(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute239(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute480(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute240(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute481(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute240(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute482(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute241(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute483(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute241(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute484(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute242(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute485(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute242(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute486(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute243(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute487(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute243(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute488(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute244(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute489(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute244(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute490(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute245(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute491(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute245(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute492(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute246(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute493(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute246(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute494(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute247(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute495(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute247(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute496(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute248(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute497(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute248(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute498(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute249(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute499(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute249(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute500(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute250(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute501(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute250(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute502(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute251(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute503(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute251(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute504(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute252(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute505(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute252(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute506(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute253(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute507(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute253(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute508(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute254(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute509(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute254(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute510(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute255(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute511(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute255(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute512(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute256(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute513(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute256(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute514(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute257(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute515(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute257(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute516(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute258(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute517(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute258(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute518(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute259(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

function compute519(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute259(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

function main() -> int32 {
  var total: int64 = (int64)0;
  total = total + compute0((int64)0, (int64)0);
  total = total + compute1((int64)1, (int64)1);
  total = total + compute2((int64)2, (int64)2);
  total = total + compute3((int64)3, (int64)3);
  total = total + compute4((int64)4, (int64)4);
  total = total + compute5((int64)5, (int64)5);
  total = total + compute6((int64)6, (int64)6);
  total = total + compute7((int64)7, (int64)7);
  total = total + compute8((int64)8, (int64)8);
  total = total + compute9((int64)9, (int64)9);
  total = total + compute10((int64)10, (int64)10);
  total = total + compute11((int64)11, (int64)11);
  total = total + compute12((int64)12, (int64)12);
  total = total + compute13((int64)13, (int64)13);
  total = total + compute14((int64)14, (int64)14);
  total = total + compute15((int64)15, (int64)15);
  total = total + compute16((int64)16, (int64)16);
  total = total + compute17((int64)17, (int64)17);
  total = total + compute18((int64)18, (int64)18);
  total = total + compute19((int64)19, (int64)19);
  total = total + compute20((int64)20, (int64)20);
  total = total + compute21((int64)21, (int64)21);
  total = total + compute22((int64)22, (int64)22);
  total = total + compute23((int64)23, (int64)23);
  total = total + compute24((int64)24, (int64)24);
  total = total + compute25((int64)25, (int64)25);
  total = total + compute26((int64)26, (int64)26);
  total = total + compute27((int64)27, (int64)27);
  total = total + compute28((int64)28, (int64)28);
  total = total + compute29((int64)29, (int64)29);
  total = total + compute30((int64)30, (int64)30);
  total = total + compute31((int64)31, (int64)31);
  total = total + compute32((int64)32, (int64)32);
  total = total + compute33((int64)33, (int64)33);
  total = total + compute34((int64)34, (int64)34);
  total = total + compute35((int64)35, (int64)35);
  total = total + compute36((int64)36, (int64)36);
  total = total + compute37((int64)37, (int64)37);
  total = total + compute38((int64)38, (int64)38);
  total = total + compute39((int64)39, (int64)39);
  total = total + compute40((int64)40, (int64)40);
  total = total + compute41((int64)41, (int64)41);
  total = total + compute42((int64)42, (int64)42);
  total = total + compute43((int64)43, (int64)43);
  total = total + compute44((int64)44, (int64)44);
  total = total + compute45((int64)45, (int64)45);
  total = total + compute46((int64)46, (int64)46);
  total = total + compute47((int64)47, (int64)47);
  total = total + compute48((int64)48, (int64)48);
  total = total + compute49((int64)49, (int64)49);
  total = total + compute50((int64)50, (int64)50);
  total = total + compute51((int64)51, (int64)51);
  total = total + compute52((int64)52, (int64)52);
  total = total + compute53((int64)53, (int64)0);
  total = total + compute54((int64)54, (int64)1);
  total = total + compute55((int64)55, (int64)2);
  total = total + compute56((int64)56, (int64)3);
  total = total + compute57((int64)57, (int64)4);
  total = total + compute58((int64)58, (int64)5);
  total = total + compute59((int64)59, (int64)6);
  total = total + compute60((int64)60, (int64)7);
  total = total + compute61((int64)61, (int64)8);
  total = total + compute62((int64)62, (int64)9);
  total = total + compute63((int64)63, (int64)10);
  total = total + compute64((int64)64, (int64)11);
  total = total + compute65((int64)65, (int64)12);
  total = total + compute66((int64)66, (int64)13);
  total = total + compute67((int64)67, (int64)14);
  total = total + compute68((int64)68, (int64)15);
  total = total + compute69((int64)69, (int64)16);
  total = total + compute70((int64)70, (int64)17);
  total = total + compute71((int64)71, (int64)18);
  total = total + compute72((int64)72, (int64)19);
  total = total + compute73((int64)73, (int64)20);
  total = total + compute74((int64)74, (int64)21);
  total = total + compute75((int64)75, (int64)22);
  total = total + compute76((int64)76, (int64)23);
  total = total + compute77((int64)77, (int64)24);
  total = total + compute78((int64)78, (int64)25);
  total = total + compute79((int64)79, (int64)26);
  total = total + compute80((int64)80, (int64)27);
  total = total + compute81((int64)81, (int64)28);
  total = total + compute82((int64)82, (int64)29);
  total = total + compute83((int64)83, (int64)30);
  total = total + compute84((int64)84, (int64)31);
  total = total + compute85((int64)85, (int64)32);
  total = total + compute86((int64)86, (int64)33);
  total = total + compute87((int64)87, (int64)34);
  total = total + compute88((int64)88, (int64)35);
  total = total + compute89((int64)89, (int64)36);
  total = total + compute90((int64)90, (int64)37);
  total = total + compute91((int64)91, (int64)38);
  total = total + compute92((int64)92, (int64)39);
  total = total + compute93((int64)93, (int64)40);
  total = total + compute94((int64)94, (int64)41);
  total = total + compute95((int64)95, (int64)42);
  total = total + compute96((int64)96, (int64)43);
  total = total + compute97((int64)0, (int64)44);
  total = total + compute98((int64)1, (int64)45);
  total = total + compute99((int64)2, (int64)46);
  total = total + compute100((int64)3, (int64)47);
  total = total + compute101((int64)4, (int64)48);
  total = total + compute102((int64)5, (int64)49);
  total = total + compute103((int64)6, (int64)50);
  total = total + compute104((int64)7, (int64)51);
  total = total + compute105((int64)8, (int64)52);
  total = total + compute106((int64)9, (int64)0);
  total = total + compute107((int64)10, (int64)1);
  total = total + compute108((int64)11, (int64)2);
  total = total + compute109((int64)12, (int64)3);
  total = total + compute110((int64)13, (int64)4);
  total = total + compute111((int64)14, (int64)5);
  total = total + compute112((int64)15, (int64)6);
  total = total + compute113((int64)16, (int64)7);
  total = total + compute114((int64)17, (int64)8);
  total = total + compute115((int64)18, (int64)9);
  total = total + compute116((int64)19, (int64)10);
  total = total + compute117((int64)20, (int64)11);
  total = total + compute118((int64)21, (int64)12);
  total = total + compute119((int64)22, (int64)13);
  total = total + compute120((int64)23, (int64)14);
  total = total + compute121((int64)24, (int64)15);
  total = total + compute122((int64)25, (int64)16);
  total = total + compute123((int64)26, (int64)17);
  total = total + compute124((int64)27, (int64)18);
  total = total + compute125((int64)28, (int64)19);
  total = total + compute126((int64)29, (int64)20);
  total = total + compute127((int64)30, (int64)21);
  total = total + compute128((int64)31, (int64)22);
  total = total + compute129((int64)32, (int64)23);
  total = total + compute130((int64)33, (int64)24);
  total = total + compute131((int64)34, (int64)25);
  total = total + compute132((int64)35, (int64)26);
  total = total + compute133((int64)36, (int64)27);
  total = total + compute134((int64)37, (int64)28);
  total = total + compute135((int64)38, (int64)29);
  total = total + compute136((int64)39, (int64)30);
  total = total + compute137((int64)40, (int64)31);
  total = total + compute138((int64)41, (int64)32);
  total = total + compute139((int64)42, (int64)33);
  total = total + compute140((int64)43, (int64)34);
  total = total + compute141((int64)44, (int64)35);
  total = total + compute142((int64)45, (int64)36);
  total = total + compute143((int64)46, (int64)37);
  total = total + compute144((int64)47, (int64)38);
  total = total + compute145((int64)48, (int64)39);
  total = total + compute146((int64)49, (int64)40);
  total = total + compute147((int64)50, (int64)41);
  total = total + compute148((int64)51, (int64)42);
  total = total + compute149((int64)52, (int64)43);
  total = total + compute150((int64)53, (int64)44);
  total = total + compute151((int64)54, (int64)45);
  total = total + compute152((int64)55, (int64)46);
  total = total + compute153((int64)56, (int64)47);
  total = total + compute154((int64)57, (int64)48);
  total = total + compute155((int64)58, (int64)49);
  total = total + compute156((int64)59, (int64)50);
  total = total + compute157((int64)60, (int64)51);
  total = total + compute158((int64)61, (int64)52);
  total = total + compute159((int64)62, (int64)0);
  total = total + compute160((int64)63, (int64)1);
  total = total + compute161((int64)64, (int64)2);
  total = total + compute162((int64)65, (int64)3);
  total = total + compute163((int64)66, (int64)4);
  total = total + compute164((int64)67, (int64)5);
  total = total + compute165((int64)68, (int64)6);
  total = total + compute166((int64)69, (int64)7);
  total = total + compute167((int64)70, (int64)8);
  total = total + compute168((int64)71, (int64)9);
  total = total + compute169((int64)72, (int64)10);
  total = total + compute170((int64)73, (int64)11);
  total = total + compute171((int64)74, (int64)12);
  total = total + compute172((int64)75, (int64)13);
  total = total + compute173((int64)76, (int64)14);
  total = total + compute174((int64)77, (int64)15);
  total = total + compute175((int64)78, (int64)16);
  total = total + compute176((int64)79, (int64)17);
  total = total + compute177((int64)80, (int64)18);
  total = total + compute178((int64)81, (int64)19);
  total = total + compute179((int64)82, (int64)20);
  total = total + compute180((int64)83, (int64)21);
  total = total + compute181((int64)84, (int64)22);
  total = total + compute182((int64)85, (int64)23);
  total = total + compute183((int64)86, (int64)24);
  total = total + compute184((int64)87, (int64)25);
  total = total + compute185((int64)88, (int64)26);
  total = total + compute186((int64)89, (int64)27);
  total = total + compute187((int64)90, (int64)28);
  total = total + compute188((int64)91, (int64)29);
  total = total + compute189((int64)92, (int64)30);
  total = total + compute190((int64)93, (int64)31);
  total = total + compute191((int64)94, (int64)32);
  total = total + compute192((int64)95, (int64)33);
  total = total + compute193((int64)96, (int64)34);
  total = total + compute194((int64)0, (int64)35);
  total = total + compute195((int64)1, (int64)36);
  total = total + compute196((int64)2, (int64)37);
  total = total + compute197((int64)3, (int64)38);
  total = total + compute198((int64)4, (int64)39);
  total = total + compute199((int64)5, (int64)40);
  total = total + compute200((int64)6, (int64)41);
  total = total + compute201((int64)7, (int64)42);
  total = total + compute202((int64)8, (int64)43);
  total = total + compute203((int64)9, (int64)44);
  total = total + compute204((int64)10, (int64)45);
  total = total + compute205((int64)11, (int64)46);
  total = total + compute206((int64)12, (int64)47);
  total = total + compute207((int64)13, (int64)48);
  total = total + compute208((int64)14, (int64)49);
  total = total + compute209((int64)15, (int64)50);
  total = total + compute210((int64)16, (int64)51);
  total = total + compute211((int64)17, (int64)52);
  total = total + compute212((int64)18, (int64)0);
  total = total + compute213((int64)19, (int64)1);
  total = total + compute214((int64)20, (int64)2);
  total = total + compute215((int64)21, (int64)3);
  total = total + compute216((int64)22, (int64)4);
  total = total + compute217((int64)23, (int64)5);
  total = total + compute218((int64)24, (int64)6);
  total = total + compute219((int64)25, (int64)7);
  total = total + compute220((int64)26, (int64)8);
  total = total + compute221((int64)27, (int64)9);
  total = total + compute222((int64)28, (int64)10);
  total = total + compute223((int64)29, (int64)11);
  total = total + compute224((int64)30, (int64)12);
  total = total + compute225((int64)31, (int64)13);
  total = total + compute226((int64)32, (int64)14);
  total = total + compute227((int64)33, (int64)15);
  total = total + compute228((int64)34, (int64)16);
  total = total + compute229((int64)35, (int64)17);
  total = total + compute230((int64)36, (int64)18);
  total = total + compute231((int64)37, (int64)19);
  total = total + compute232((int64)38, (int64)20);
  total = total + compute233((int64)39, (int64)21);
  total = total + compute234((int64)40, (int64)22);
  total = total + compute235((int64)41, (int64)23);
  total = total + compute236((int64)42, (int64)24);
  total = total + compute237((int64)43, (int64)25);
  total = total + compute238((int64)44, (int64)26);
  total = total + compute239((int64)45, (int64)27);
  total = total + compute240((int64)46, (int64)28);
  total = total + compute241((int64)47, (int64)29);
  total = total + compute242((int64)48, (int64)30);
  total = total + compute243((int64)49, (int64)31);
  total = total + compute244((int64)50, (int64)32);
  total = total + compute245((int64)51, (int64)33);
  total = total + compute246((int64)52, (int64)34);
  total = total + compute247((int64)53, (int64)35);
  total = total + compute248((int64)54, (int64)36);
  total = total + compute249((int64)55, (int64)37);
  total = total + compute250((int64)56, (int64)38);
  total = total + compute251((int64)57, (int64)39);
  total = total + compute252((int64)58, (int64)40);
  total = total + compute253((int64)59, (int64)41);
  total = total + compute254((int64)60, (int64)42);
  total = total + compute255((int64)61, (int64)43);
  total = total + compute256((int64)62, (int64)44);
  total = total + compute257((int64)63, (int64)45);
  total = total + compute258((int64)64, (int64)46);
  total = total + compute259((int64)65, (int64)47);
  total = total + compute260((int64)66, (int64)48);
  total = total + compute261((int64)67, (int64)49);
  total = total + compute262((int64)68, (int64)50);
  total = total + compute263((int64)69, (int64)51);
  total = total + compute264((int64)70, (int64)52);
  total = total + compute265((int64)71, (int64)0);
  total = total + compute266((int64)72, (int64)1);
  total = total + compute267((int64)73, (int64)2);
  total = total + compute268((int64)74, (int64)3);
  total = total + compute269((int64)75, (int64)4);
  total = total + compute270((int64)76, (int64)5);
  total = total + compute271((int64)77, (int64)6);
  total = total + compute272((int64)78, (int64)7);
  total = total + compute273((int64)79, (int64)8);
  total = total + compute274((int64)80, (int64)9);
  total = total + compute275((int64)81, (int64)10);
  total = total + compute276((int64)82, (int64)11);
  total = total + compute277((int64)83, (int64)12);
  total = total + compute278((int64)84, (int64)13);
  total = total + compute279((int64)85, (int64)14);
  total = total + compute280((int64)86, (int64)15);
  total = total + compute281((int64)87, (int64)16);
  total = total + compute282((int64)88, (int64)17);
  total = total + compute283((int64)89, (int64)18);
  total = total + compute284((int64)90, (int64)19);
  total = total + compute285((int64)91, (int64)20);
  total = total + compute286((int64)92, (int64)21);
  total = total + compute287((int64)93, (int64)22);
  total = total + compute288((int64)94, (int64)23);
  total = total + compute289((int64)95, (int64)24);
  total = total + compute290((int64)96, (int64)25);
  total = total + compute291((int64)0, (int64)26);
  total = total + compute292((int64)1, (int64)27);
  total = total + compute293((int64)2, (int64)28);
  total = total + compute294((int64)3, (int64)29);
  total = total + compute295((int64)4, (int64)30);
  total = total + compute296((int64)5, (int64)31);
  total = total + compute297((int64)6, (int64)32);
  total = total + compute298((int64)7, (int64)33);
  total = total + compute299((int64)8, (int64)34);
  total = total + compute300((int64)9, (int64)35);
  total = total + compute301((int64)10, (int64)36);
  total = total + compute302((int64)11, (int64)37);
  total = total + compute303((int64)12, (int64)38);
  total = total + compute304((int64)13, (int64)39);
  total = total + compute305((int64)14, (int64)40);
  total = total + compute306((int64)15, (int64)41);
  total = total + compute307((int64)16, (int64)42);
  total = total + compute308((int64)17, (int64)43);
  total = total + compute309((int64)18, (int64)44);
  total = total + compute310((int64)19, (int64)45);
  total = total + compute311((int64)20, (int64)46);
  total = total + compute312((int64)21, (int64)47);
  total = total + compute313((int64)22, (int64)48);
  total = total + compute314((int64)23, (int64)49);
  total = total + compute315((int64)24, (int64)50);
  total = total + compute316((int64)25, (int64)51);
  total = total + compute317((int64)26, (int64)52);
  total = total + compute318((int64)27, (int64)0);
  total = total + compute319((int64)28, (int64)1);
  total = total + compute320((int64)29, (int64)2);
  total = total + compute321((int64)30, (int64)3);
  total = total + compute322((int64)31, (int64)4);
  total = total + compute323((int64)32, (int64)5);
  total = total + compute324((int64)33, (int64)6);
  total = total + compute325((int64)34, (int64)7);
  total = total + compute326((int64)35, (int64)8);
  total = total + compute327((int64)36, (int64)9);
  total = total + compute328((int64)37, (int64)10);
  total = total + compute329((int64)38, (int64)11);
  total = total + compute330((int64)39, (int64)12);
  total = total + compute331((int64)40, (int64)13);
  total = total + compute332((int64)41, (int64)14);
  total = total + compute333((int64)42, (int64)15);
  total = total + compute334((int64)43, (int64)16);
  total = total + compute335((int64)44, (int64)17);
  total = total + compute336((int64)45, (int64)18);
  total = total + compute337((int64)46, (int64)19);
  total = total + compute338((int64)47, (int64)20);
  total = total + compute339((int64)48, (int64)21);
  total = total + compute340((int64)49, (int64)22);
  total = total + compute341((int64)50, (int64)23);
  total = total + compute342((int64)51, (int64)24);
  total = total + compute343((int64)52, (int64)25);
  total = total + compute344((int64)53, (int64)26);
  total = total + compute345((int64)54, (int64)27);
  total = total + compute346((int64)55, (int64)28);
  total = total + compute347((int64)56, (int64)29);
  total = total + compute348((int64)57, (int64)30);
  total = total + compute349((int64)58, (int64)31);
  total = total + compute350((int64)59, (int64)32);
  total = total + compute351((int64)60, (int64)33);
  total = total + compute352((int64)61, (int64)34);
  total = total + compute353((int64)62, (int64)35);
  total = total + compute354((int64)63, (int64)36);
  total = total + compute355((int64)64, (int64)37);
  total = total + compute356((int64)65, (int64)38);
  total = total + compute357((int64)66, (int64)39);
  total = total + compute358((int64)67, (int64)40);
  total = total + compute359((int64)68, (int64)41);
  total = total + compute360((int64)69, (int64)42);
  total = total + compute361((int64)70, (int64)43);
  total = total + compute362((int64)71, (int64)44);
  total = total + compute363((int64)72, (int64)45);
  total = total + compute364((int64)73, (int64)46);
  total = total + compute365((int64)74, (int64)47);
  total = total + compute366((int64)75, (int64)48);
  total = total + compute367((int64)76, (int64)49);
  total = total + compute368((int64)77, (int64)50);
  total = total + compute369((int64)78, (int64)51);
  total = total + compute370((int64)79, (int64)52);
  total = total + compute371((int64)80, (int64)0);
  total = total + compute372((int64)81, (int64)1);
  total = total + compute373((int64)82, (int64)2);
  total = total + compute374((int64)83, (int64)3);
  total = total + compute375((int64)84, (int64)4);
  total = total + compute376((int64)85, (int64)5);
  total = total + compute377((int64)86, (int64)6);
  total = total + compute378((int64)87, (int64)7);
  total = total + compute379((int64)88, (int64)8);
  total = total + compute380((int64)89, (int64)9);
  total = total + compute381((int64)90, (int64)10);
  total = total + compute382((int64)91, (int64)11);
  total = total + compute383((int64)92, (int64)12);
  total = total + compute384((int64)93, (int64)13);
  total = total + compute385((int64)94, (int64)14);
  total = total + compute386((int64)95, (int64)15);
  total = total + compute387((int64)96, (int64)16);
  total = total + compute388((int64)0, (int64)17);
  total = total + compute389((int64)1, (int64)18);
  total = total + compute390((int64)2, (int64)19);
  total = total + compute391((int64)3, (int64)20);
  total = total + compute392((int64)4, (int64)21);
  total = total + compute393((int64)5, (int64)22);
  total = total + compute394((int64)6, (int64)23);
  total = total + compute395((int64)7, (int64)24);
  total = total + compute396((int64)8, (int64)25);
  total = total + compute397((int64)9, (int64)26);
  total = total + compute398((int64)10, (int64)27);
  total = total + compute399((int64)11, (int64)28);
  total = total + compute400((int64)12, (int64)29);
  total = total + compute401((int64)13, (int64)30);
  total = total + compute402((int64)14, (int64)31);
  total = total + compute403((int64)15, (int64)32);
  total = total + compute404((int64)16, (int64)33);
  total = total + compute405((int64)17, (int64)34);
  total = total + compute406((int64)18, (int64)35);
  total = total + compute407((int64)19, (int64)36);
  total = total + compute408((int64)20, (int64)37);
  total = total + compute409((int64)21, (int64)38);
  total = total + compute410((int64)22, (int64)39);
  total = total + compute411((int64)23, (int64)40);
  total = total + compute412((int64)24, (int64)41);
  total = total + compute413((int64)25, (int64)42);
  total = total + compute414((int64)26, (int64)43);
  total = total + compute415((int64)27, (int64)44);
  total = total + compute416((int64)28, (int64)45);
  total = total + compute417((int64)29, (int64)46);
  total = total + compute418((int64)30, (int64)47);
  total = total + compute419((int64)31, (int64)48);
  total = total + compute420((int64)32, (int64)49);
  total = total + compute421((int64)33, (int64)50);
  total = total + compute422((int64)34, (int64)51);
  total = total + compute423((int64)35, (int64)52);
  total = total + compute424((int64)36, (int64)0);
  total = total + compute425((int64)37, (int64)1);
  total = total + compute426((int64)38, (int64)2);
  total = total + compute427((int64)39, (int64)3);
  total = total + compute428((int64)40, (int64)4);
  total = total + compute429((int64)41, (int64)5);
  total = total + compute430((int64)42, (int64)6);
  total = total + compute431((int64)43, (int64)7);
  total = total + compute432((int64)44, (int64)8);
  total = total + compute433((int64)45, (int64)9);
  total = total + compute434((int64)46, (int64)10);
  total = total + compute435((int64)47, (int64)11);
  total = total + compute436((int64)48, (int64)12);
  total = total + compute437((int64)49, (int64)13);
  total = total + compute438((int64)50, (int64)14);
  total = total + compute439((int64)51, (int64)15);
  total = total + compute440((int64)52, (int64)16);
  total = total + compute441((int64)53, (int64)17);
  total = total + compute442((int64)54, (int64)18);
  total = total + compute443((int64)55, (int64)19);
  total = total + compute444((int64)56, (int64)20);
  total = total + compute445((int64)57, (int64)21);
  total = total + compute446((int64)58, (int64)22);
  total = total + compute447((int64)59, (int64)23);
  total = total + compute448((int64)60, (int64)24);
  total = total + compute449((int64)61, (int64)25);
  total = total + compute450((int64)62, (int64)26);
  total = total + compute451((int64)63, (int64)27);
  total = total + compute452((int64)64, (int64)28);
  total = total + compute453((int64)65, (int64)29);
  total = total + compute454((int64)66, (int64)30);
  total = total + compute455((int64)67, (int64)31);
  total = total + compute456((int64)68, (int64)32);
  total = total + compute457((int64)69, (int64)33);
  total = total + compute458((int64)70, (int64)34);
  total = total + compute459((int64)71, (int64)35);
  total = total + compute460((int64)72, (int64)36);
  total = total + compute461((int64)73, (int64)37);
  total = total + compute462((int64)74, (int64)38);
  total = total + compute463((int64)75, (int64)39);
  total = total + compute464((int64)76, (int64)40);
  total = total + compute465((int64)77, (int64)41);
  total = total + compute466((int64)78, (int64)42);
  total = total + compute467((int64)79, (int64)43);
  total = total + compute468((int64)80, (int64)44);
  total = total + compute469((int64)81, (int64)45);
  total = total + compute470((int64)82, (int64)46);
  total = total + compute471((int64)83, (int64)47);
  total = total + compute472((int64)84, (int64)48);
  total = total + compute473((int64)85, (int64)49);
  total = total + compute474((int64)86, (int64)50);
  total = total + compute475((int64)87, (int64)51);
  total = total + compute476((int64)88, (int64)52);
  total = total + compute477((int64)89, (int64)0);
  total = total + compute478((int64)90, (int64)1);
  total = total + compute479((int64)91, (int64)2);
  total = total + compute480((int64)92, (int64)3);
  total = total + compute481((int64)93, (int64)4);
  total = total + compute482((int64)94, (int64)5);
  total = total + compute483((int64)95, (int64)6);
  total = total + compute484((int64)96, (int64)7);
  total = total + compute485((int64)0, (int64)8);
  total = total + compute486((int64)1, (int64)9);
  total = total + compute487((int64)2, (int64)10);
  total = total + compute488((int64)3, (int64)11);
  total = total + compute489((int64)4, (int64)12);
  total = total + compute490((int64)5, (int64)13);
  total = total + compute491((int64)6, (int64)14);
  total = total + compute492((int64)7, (int64)15);
  total = total + compute493((int64)8, (int64)16);
  total = total + compute494((int64)9, (int64)17);
  total = total + compute495((int64)10, (int64)18);
  total = total + compute496((int64)11, (int64)19);
  total = total + compute497((int64)12, (int64)20);
  total = total + compute498((int64)13, (int64)21);
  total = total + compute499((int64)14, (int64)22);
  total = total + compute500((int64)15, (int64)23);
  total = total + compute501((int64)16, (int64)24);
  total = total + compute502((int64)17, (int64)25);
  total = total + compute503((int64)18, (int64)26);
  total = total + compute504((int64)19, (int64)27);
  total = total + compute505((int64)20, (int64)28);
  total = total + compute506((int64)21, (int64)29);
  total = total + compute507((int64)22, (int64)30);
  total = total + compute508((int64)23, (int64)31);
  total = total + compute509((int64)24, (int64)32);
  total = total + compute510((int64)25, (int64)33);
  total = total + compute511((int64)26, (int64)34);
  total = total + compute512((int64)27, (int64)35);
  total = total + compute513((int64)28, (int64)36);
  total = total + compute514((int64)29, (int64)37);
  total = total + compute515((int64)30, (int64)38);
  total = total + compute516((int64)31, (int64)39);
  total = total + compute517((int64)32, (int64)40);
  total = total + compute518((int64)33, (int64)41);
  total = total + compute519((int64)34, (int64)42);
  return (int32)(total % (int64)256);
}
