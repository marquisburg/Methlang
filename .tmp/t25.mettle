import "std/mem";
function bump_byte(b: cstring, i: int64) -> int64 {
  var v: int64 = (int64)(uint8)b[i];
  b[i] = (uint8)(v + 1);
  return v;
}
function main() -> int32 {
  var b: cstring = malloc(16);
  memset(b, 0, 16);
  var r: int64 = bump_byte(b, 1);
  free(b);
  return (int32)r;
}
