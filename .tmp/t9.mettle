import "std/io";
import "std/mem";
function main() -> int32 {
  var b: cstring = malloc(262144);
  memset(b, 0, 262144);
  var i: int64 = 0;
  while (i < 8193) {
    var v: int64 = (int64)(uint8)b[i];
    b[i] = (uint8)(v + 1);
    i = i + 8192;
  }
  println(cstr("ok"));
  free(b);
  return 0;
}
