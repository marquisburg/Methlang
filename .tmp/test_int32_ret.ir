; Generated by Mettle
; x86-64 Assembly Output

bits 64

section .text
; Code section

; First pass: processing 2 declarations
; Declaration 0 type: 4 (AST_INLINE_ASM = 24)
; Declaration 1 type: 4 (AST_INLINE_ASM = 24)

section .text

global callee

callee:
    push rbp        ; Save old base pointer
    mov rbp, rsp  ; Set new base pointer
    sub rsp, 32    ; Allocate 32 bytes on stack (aligned)
    ; Registering 0 function parameters
ir_entry_0:
    mov rax, 1
    mov [rbp - 8], rax
    cmp qword [rbp - 8], 0
    jnz ir_errdefer_end_2
ir_errdefer_ok_1:
ir_errdefer_end_2:
    mov rax, 1
    jmp Lcallee_exit
Lcallee_exit:
    ; Function epilogue
    mov rsp, rbp  ; Restore stack pointer
    pop rbp         ; Restore old base pointer
    ret               ; Return to caller

global main

main:
    push rbp        ; Save old base pointer
    mov rbp, rsp  ; Set new base pointer
    sub rsp, 96    ; Allocate 96 bytes on stack (aligned)
    ; Registering 0 function parameters
    mov [rbp - 8], r12
ir_entry_3:
    mov rax, 1
    ; Store to variable: ok
    mov r12d, eax       ; To register (int32/uint32)
    ; IR call: callee (0 args)
    call callee
    ; Integer/pointer return value in rax
    ; 32-bit return value already in eax
    cmp rax, 0
    jne ir_if_next_5
    mov rax, 0
    ; Store to variable: ok
    mov r12d, eax       ; To register (int32/uint32)
    jmp ir_if_end_4
ir_if_next_5:
ir_if_end_4:
    ; Load variable: ok
    movsxd rax, r12d      ; From register (signed int32)
    mov [rbp - 32], rax
    cmp qword [rbp - 32], 0
    jnz ir_errdefer_end_7
ir_errdefer_ok_6:
ir_errdefer_end_7:
    ; Load variable: ok
    movsxd rax, r12d      ; From register (signed int32)
    jmp Lmain_exit
Lmain_exit:
    mov r12, [rbp - 8]
    ; Function epilogue
    mov rsp, rbp  ; Restore stack pointer
    pop rbp         ; Restore old base pointer
    ret               ; Return to caller

; Default program entry point
global mainCRTStartup
mainCRTStartup:
    sub rsp, 40      ; Shadow space + alignment
    ; Call user main function
    call main
    mov ecx, eax     ; Use main return as exit code
    extern ExitProcess
    call ExitProcess
