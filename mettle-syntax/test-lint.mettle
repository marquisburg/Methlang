// Intentional linter test file - triggers mettle-syntax diagnostics.
// Open this file and check View > Problems.

function main() -> int32 {
  var bad_hex: int32 = 0x;         // invalid hex - no digits
  var bad_bin: int32 = 0b;         // invalid binary - no digits
  var bad_hex2: int32 = 0Xg;       // invalid hex - g not allowed

  var big: int64 = 1_000_000;      // underscores in numbers are not supported

  // These are supported and should not warn:
  /* block comment */
  outer: for (var i: int32 = 0; i < 4; i += 1) {
    if (i == 2) {
      break outer;
    }
  }

  return bad_hex + bad_bin + bad_hex2 + (int32)big;
}

/* unterminated block comment
