// Valid Mettle — use this to verify meth-syntax compiler diagnostics are clean on save.
// Expect no Problems when `meth.linter.compilerEnabled` is true and `mettle` resolves.

function main() -> int32 {
  return 0;
}
